class Track{
    length; //length of the song
    artist; //name of the artist
    name;

    constructor(length, artist, name){
        this.length = length;
        this.artist = artist;
        this.name = name;
    }
}